let numbers = parseInt(prompt("nhập nguyên tố cần kiểm tra"));
if (isNaN(numbers)) {
    console.log("nhập 1 số nguyên hợp lệ");
} else {
    let count = 0;
    for (let i = 2; count < numbers);
}